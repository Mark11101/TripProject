CREATE TABLE `Bars_schema`.`BarsTable` (
                                     `ID` INT NOT NULL,
                                     `Name` VARCHAR(45) NOT NULL,
                                     `Rating` VARCHAR(45) NOT NULL,
                                     `Address` VARCHAR(45) NOT NULL,
                                     PRIMARY KEY (`ID`));
CREATE TABLE `Bars_schema`.`ParkTable` (
                                     `ID` INT NOT NULL,
                                     `Name` VARCHAR(45) NOT NULL,
                                     `Rating` VARCHAR(45) NOT NULL,
                                     `Address` VARCHAR(45) NOT NULL,
                                     PRIMARY KEY (`ID`));
CREATE TABLE `Bars_schema`.`RestsTable` (
                                           `ID` INT NOT NULL,
                                           `Name` VARCHAR(45) NOT NULL,
                                           `Rating` VARCHAR(45) NOT NULL,
                                           `Address` VARCHAR(45) NOT NULL,
                                           PRIMARY KEY (`ID`));
CREATE TABLE `Bars_schema`.`CinemasTable` (
                                           `ID` INT NOT NULL,
                                           `Name` VARCHAR(45) NOT NULL,
                                           `Rating` VARCHAR(45) NOT NULL,
                                           `Address` VARCHAR(45) NOT NULL,
                                           PRIMARY KEY (`ID`));
CREATE TABLE `Bars_schema`.`TCTTable` (
                                           `ID` INT NOT NULL,
                                           `Name` VARCHAR(45) NOT NULL,
                                           `Rating` VARCHAR(45) NOT NULL,
                                           `Address` VARCHAR(45) NOT NULL,
                                           PRIMARY KEY (`ID`));
CREATE TABLE `Bars_schema`.`TheatersTable` (
                                           `ID` INT NOT NULL,
                                           `Name` VARCHAR(45) NOT NULL,
                                           `Rating` VARCHAR(45) NOT NULL,
                                           `Address` VARCHAR(45) NOT NULL,
                                           PRIMARY KEY (`ID`));
